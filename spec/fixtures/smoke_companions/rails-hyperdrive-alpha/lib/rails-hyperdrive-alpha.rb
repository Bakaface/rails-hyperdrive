# Smoke-test companion gem (alpha). Intentionally a no-op at runtime — its only
# purpose is to ship hyperdrive skills/guidelines under lib/<gem>/hyperdrive/
# so the install generator can discover them from a real bundle.
module RailsHyperdriveAlpha
end
